@model RecursosHumanos.Models.Departamento

@{
    ViewBag.Title = "Delete";
}

<link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.1/css/all.css" integrity="sha384-50oBUHEmvpQ+1lW4y57PTFmhCaXp0ML5d60M1M7uH2+nqUivzIebhndOJK28anvf" crossorigin="anonymous">
<link href="https://fonts.googleapis.com/css?family=Montserrat" rel="stylesheet">
<link href="https://fonts.googleapis.com/css?family=Oswald" rel="stylesheet">
@Styles.Render("~/Content/estilos.css")

<div class="contenedor-modulo contenedor">
    <h2 class="margin">Departamentos</h2>

    <h3>¿Seguro que quieres eliminar este departamento?</h3>

    <div class="contenedor-tablas">

        <table class="table">
            <tr class="tabla-header">
                <th>
                    @Html.DisplayNameFor(model => model.Codigo_Departamnto)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Nombre)
                </th>
            </tr>

            <tr>
                <td>
                    @Html.DisplayFor(model => model.Codigo_Departamnto)
                </td>
                <td>
                    @Html.DisplayFor(model => model.Nombre)
                </td>
            </tr>
        </table>


        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="opciones">
                <a href="@Url.Action("Index")" class="boton"><i class="fas fa-user-times"></i> Cancelar</a>
                <button type="submit" class="boton"><i class="fas fa-trash-alt"></i> Eliminar</button>
            </div><!--OPCIONES-->
        }
    </div>
</div>