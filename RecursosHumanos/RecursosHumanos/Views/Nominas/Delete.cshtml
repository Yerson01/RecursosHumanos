@model RecursosHumanos.Models.Nomina

@{
    ViewBag.Title = "Delete";
}

<link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.1/css/all.css" integrity="sha384-50oBUHEmvpQ+1lW4y57PTFmhCaXp0ML5d60M1M7uH2+nqUivzIebhndOJK28anvf" crossorigin="anonymous">
<link href="https://fonts.googleapis.com/css?family=Montserrat" rel="stylesheet">
<link href="https://fonts.googleapis.com/css?family=Oswald" rel="stylesheet">
@Styles.Render("~/Content/estilos.css")

<div class="contenedor-modulo contenedor">
    <h2 class="margin">Nómina</h2>

    <h3>¿Seguro que quieres eliminar esta nómina?</h3>

    <div class="contenedor-tablas">

        <table class="table">
            <tr class="tabla-header">
                <th>
                    @Html.DisplayNameFor(model => model.Monto_Total)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Dia)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Mes)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Año)
                </th>
            </tr>

            <tr>
                <td>
                   RD$ @Html.DisplayFor(model => model.Monto_Total)
                </td>
                <td>
                    @Html.DisplayFor(model => model.Dia)
                </td>
                <td>
                    @Html.DisplayFor(model => model.Mes)
                </td>
                <td>
                    @Html.DisplayFor(model => model.Año)
                </td>
            </tr>
        </table>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="opciones">
                <a href="@Url.Action("Index")" class="boton"><i class="fas fa-user-times"></i> Cancelar</a>
                <button type="submit" class="boton"><i class="fas fa-trash-alt"></i> Eliminar</button>
            </div><!--OPCIONES-->
        }
    </div>
</div>